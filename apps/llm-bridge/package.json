{
  "name": "llm-bridge",
  "version": "0.1.0",
  "private": true,
  "description": "Local LLM bridge: turns structured requests from local apps into controlled, tool-less Claude Code invocations. No API key required. HomeAIR is its first customer; it has no dependency on HomeAIR.",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "typecheck": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "dotenv": "^16.4.7",
    "fastify": "^5.2.1",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.7",
    "tsx": "^4.19.2"
  }
}
